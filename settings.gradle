rootProject.name = 'batch-boilerplate'

include 'common'

include 'apps:avc-batch'
findProject(':apps:avc-batch')?.name = 'avc-batch'

include 'repositories:avc-persistence'
findProject(':repositories:avc-persistence')?.name = 'avc-persistence'